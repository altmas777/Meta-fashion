export const metadata = {
  title: "Create Account",
  description: "Join the exclusive circle at Meta Fashion.",
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}
