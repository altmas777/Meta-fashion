export const metadata = {
  title: "Shopping Cart",
  description: "View and checkout your selected premium items at Meta Fashion.",
};

export default function CartLayout({ children }) {
  return <>{children}</>;
}
